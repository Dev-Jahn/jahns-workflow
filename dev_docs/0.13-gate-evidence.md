# 0.13 intent control plane exit gate evidence

- Gate task: `gate/013-intent-control-plane`
- 검증 대상: `b0635bdfd22a6b8022b6de1eaf45923c44e8a840`
- 구현 전 diff 기준: `cecb7b34b85a31d0048c12a1bfacb64d3488b702`
- 검증일: 2026-07-22 (Asia/Seoul)
- 종합 판정: **FAIL**

이 판정은 test 수나 finding 수를 성공 지표로 사용하지 않는다. §9.1의 product contract 중 4번은
production review-cycle head가 영속되지 않고, 5번과 6번에는 대표 happy path가 없으며, 1회의
real-backend smoke도 marker 이전에 실패했기 때문이다. §9.2
상한을 지켜 전체 suite와 real-backend smoke는 각각 한 번만 실행했으며 model/profile/stage matrix,
suite 재실행, smoke 재시도는 하지 않았다.

## 1. 핵심 e2e 7

기존 focused test를 인용하는 모든 항목은 아래 **한 번의 동일 suite 실행**에서 관측했다.

```console
$ uv run scripts/tests/run_tests.py
Ran 224 tests in 70.645s
OK
$ echo $?
0
```

아래 `관측 digest`는 각 항목에 적은 canonical observation preimage를
`printf %s '<preimage>' | shasum -a 256`으로 계산한 검증 manifest digest다. product CAS artifact가
아닌, 어떤 test와 정적 관측으로 판정했는지를 고정하는 digest다.

| # | product contract | 판정 | command exit | 관측 digest |
|---|---|---|---:|---|
| 1 | Semantic prompt | **PASS** | 0 | `sha256:2e4f001e31e5c3c094ba0ad425b6af3b7559268ba8e55ab03865a0ca752b2a79` |
| 2 | Context resume | **PASS** | 0 | `sha256:edf16295cfbe14e7bdde9444c67df1f8891a17be8ab436bd54468f9d5ea77093` |
| 3 | Truth != priority | **PASS** | 0 | `sha256:26c297245dd6653f20f894dac4f3a81ab384de6ad425e2fd5a8c6697c20307f0` |
| 4 | Promotion boundary | **FAIL** | 0 | `sha256:e907b47b80a7dfde51f1bad485fab829597b6d208cba443b4b4cac94457d1e1a` |
| 5 | Cheap exploration | **FAIL** | 0 | `sha256:79947f76edbaef3d6140687757ffd010bed15eac2c0f28f9e35afca6ee2d6feb` |
| 6 | Expensive promotion | **FAIL** | 0 | `sha256:8d562c41d41711710d597702aebc8e9ca5c0d0de47ae060722208ec8c2f7e0b0` |
| 7 | Outcome progress | **PASS** | 0 | `sha256:79746aba57f34de1478e6cedcdfbba7d916d75bac20a73bd7ff64b73f5229e13` |

### 1. Semantic prompt — PASS

Command는 위 suite 1회 실행이다. 다음 focused test가 모두 `ok`였다.

- `test_work_brief.WorkBriefTests.test_semantic_prompt_has_seven_sections_and_no_bookkeeping_protocol`
- `test_work_brief.WorkBriefTests.test_every_provenance_source_must_be_digest_bound`

첫 test는 architecture brief를 7개 semantic section으로 렌더하고 why/current state/fixed
decisions/worker freedom/provenance가 존재하는 한편 `tasks.yaml`, round, overlay, lease, artifact naming
protocol이 없는 것을 동시에 판별한다. 두 번째 test는 provenance source가 digest-bound임을 판별한다.

Observation preimage:

```text
E2E-1|test_work_brief.WorkBriefTests.test_semantic_prompt_has_seven_sections_and_no_bookkeeping_protocol=ok|test_work_brief.WorkBriefTests.test_every_provenance_source_must_be_digest_bound=ok|verdict=PASS
```

### 2. Context resume — PASS

Command는 위 suite 1회 실행이다. 다음 happy/negative가 모두 `ok`였다.

- `test_run_context.ContextResumeE2ETests.test_response_publishes_new_brief_spec_and_attempt`
- `test_run_context.RunContextTests.test_context_request_and_response_create_new_revision_attempt_without_resetting_budget`
- `test_run_context.RunContextTests.test_cancel_and_response_race_has_one_winner_and_loser_leaves_no_references`
- `test_worker_result.WorkerResultTests.test_context_request_with_code_delta_is_rejected`
- `test_worker_result.WorkerResultTests.test_stale_coordinator_binding_digest_is_rejected`

새 WorkBrief digest, RunSpec revision, attempt가 생성되고 budget이 reset되지 않는다. code/config delta가
있는 context request와 stale coordinator binding은 거부되며 cancel/response race 패자는
`context_not_current`이고 reference를 남기지 않는다. crash-before/after publication fixture도 같은 suite에서
통과했다.

Observation preimage:

```text
E2E-2|test_run_context.ContextResumeE2ETests.test_response_publishes_new_brief_spec_and_attempt=ok|test_run_context.RunContextTests.test_cancel_and_response_race_has_one_winner_and_loser_leaves_no_references=ok|test_worker_result.WorkerResultTests.test_context_request_with_code_delta_is_rejected=ok|verdict=PASS
```

### 3. Truth != priority — PASS

Command는 위 suite 1회 실행이다. 다음 test가 모두 `ok`였다.

- `test_review_findings.FindingChainTests.test_confirmed_major_accept_risk_does_not_materialize`
- `test_review_findings.FindingChainTests.test_q3_owner_only_boundaries`

`backlog`과 `accept-risk`는 canonical disposition union에 속하지만 selected executable work가 아니므로
materialize할 수 없다. `confirmed major`의 존재만으로 task나 전역 block이 생기지 않고, owner-only
accept-risk 경계를 coordinator가 위조하지 못한다.

Observation preimage:

```text
E2E-3|test_review_findings.FindingChainTests.test_confirmed_major_accept_risk_does_not_materialize=ok|test_review_findings.FindingChainTests.test_q3_owner_only_boundaries=ok|verdict=PASS
```

### 4. Promotion boundary — FAIL

Suite command는 위 1회 실행이고, production head 운반을 확인한 추가 read-only command는 다음과 같다.

```console
$ nl -ba waystone/runs/engine.py | sed -n '716,750p'; nl -ba waystone/cli/run_group.py | sed -n '315,365p'; rg -n 'append_review_cycle\(|review_cycle_head_digest' waystone scripts/tests
# exit 0
```

다음 focused test가 모두 `ok`였다.

- `test_run_assurance.PromotionLineageTests.test_fix_before_promotion_blocks_until_verified_descendant_clearance`
- `test_run_assurance.PromotionLineageTests.test_review_cycle_count_rederives_and_cannot_be_reset`
- `test_run_assurance.CandidateEvaluationTests.test_evaluate_and_promote_runspec_freeze_one_candidate_generation`

첫 test는 exact promotion lineage만 차단하고 descendant candidate에서 failure mechanism에 직접 결속된
독립 verifier evidence가 있어야 clearance되는 것을 판별한다. 두 번째 test는 **공급된** immutable cycle
chain에서 count를 재도출하고 caller가 낮춘 count를 거부한다. candidate/spec generation freeze도 별도
focused test에서 판별된다. 그러나 production `append_review_cycle`은 새 cycle head를 durable lineage에
연결하지 않고, 새 promote run은 evaluation producer의 여전히 null인 head를 다시 상속한다. 새
candidate/run으로 review-cycle count를 우회할 수 있으므로 §9.1 문장 전체는 FAIL이다. 상세 claim은
`G013-03`에 기록한다.

Observation preimage:

```text
E2E-4|test_run_assurance.PromotionLineageTests.test_fix_before_promotion_blocks_until_verified_descendant_clearance=ok|test_run_assurance.PromotionLineageTests.test_review_cycle_count_rederives_and_cannot_be_reset=ok|test_run_assurance.CandidateEvaluationTests.test_evaluate_and_promote_runspec_freeze_one_candidate_generation=ok|production-cycle-head-not-persisted|verdict=FAIL
```

### 5. Cheap exploration — FAIL

Command와 exit code:

```console
$ uv run scripts/tests/run_tests.py
# exit 0; 위 1회 실행을 공유함
$ nl -ba skills/run/SKILL.md | sed -n '1,80p'; nl -ba waystone/cli/run_group.py | sed -n '484,508p'; nl -ba waystone/runs/engine.py | sed -n '460,580p'
# exit 0
```

`test_run_assurance.AssurancePlanTests.test_check_free_explore_preflight_definition_is_valid`와
`test_run_assurance.CandidateEvaluationTests.test_candidate_descriptor_is_content_bound_and_supersedes_is_explicit`
는 각각 check-free explore plan과 immutable candidate descriptor를 판별했다. 그러나 대표 happy
explore worker run의 완주는 판별하지 않는다.

정적 관측상 `waystone run start`는 spec/attempt를 `dispatch-ready`/`running`으로 만들고 즉시 반환한다
(`run_group.py:484-499`). `run resume`는 transport projection만 반환한다(`run_group.py:501-508`,
`engine.py:460-461`). 실제 stage 실행은 caller-supplied handler를 요구하는 비공개 Python seam
`StagedRunEngine.execute_stage`뿐이고(`engine.py:467-580`), `skills/run/SKILL.md`에도 그 seam으로
연결하는 단계가 없다. 또한 completed result의 outcome lineage 누락은 `G013-02`에 기록했다. 따라서
locally reachable candidate + temporary probe/refuted hypothesis + close까지의 대표 happy path가 없으며
이 문장을 PASS로 판정할 수 없다.

Observation preimage:

```text
E2E-5|test_run_assurance.AssurancePlanTests.test_check_free_explore_preflight_definition_is_valid=ok|test_run_assurance.CandidateEvaluationTests.test_candidate_descriptor_is_content_bound_and_supersedes_is_explicit=ok|public-start-does-not-dispatch-runner|completed-worker-result-reference-absent|verdict=FAIL
```

### 6. Expensive promotion — FAIL

Command와 exit code:

```console
$ uv run scripts/tests/run_tests.py
# exit 0; 위 1회 실행을 공유함
$ nl -ba waystone/runs/engine.py | sed -n '490,580p'; nl -ba waystone/jobs/completion.py | sed -n '565,654p'; nl -ba scripts/tests/test_run_assurance.py | sed -n '217,390p'
# exit 0
```

다음 negative contract는 모두 `ok`였다.

- `test_run_assurance.AssurancePlanTests.test_each_stage_executes_only_frozen_actions_and_evaluate_mutation_refuses`
- `test_completion_contract.CompletionContractTests.test_promotion_allows_committed_prototype_but_refuses_hypothesis`
- `test_run_outcome.RunOutcomeTests.test_evaluate_property_claim_without_verifier_evidence_is_refused`
- `test_run_assurance.CandidateEvaluationTests.test_evaluate_and_promote_runspec_freeze_one_candidate_generation`

코드는 committed frame, regression/supported-scope/accepted-risk CAS record, exact candidate/evaluation
lineage를 요구하고 evaluate mutation과 hypothesis promotion criterion을 typed refusal한다. 그러나 마지막
test의 promote 부분은 required record가 없어서 handler 호출 전에 거부되는 **negative**이고, 동일
candidate/spec을 read-only evaluate한 뒤 reviewer evidence와 최소 record를 갖추어 implementer mutation 없이
apply/complete하는 대표 happy path가 아니다. public execution seam도 5번과 같이 끊겨 있다. 따라서
§9.1이 요구한 happy + discriminating negative의 절반만 성립해 FAIL이다.

Observation preimage:

```text
E2E-6|test_run_assurance.AssurancePlanTests.test_each_stage_executes_only_frozen_actions_and_evaluate_mutation_refuses=ok|test_completion_contract.CompletionContractTests.test_promotion_allows_committed_prototype_but_refuses_hypothesis=ok|test_run_outcome.RunOutcomeTests.test_evaluate_property_claim_without_verifier_evidence_is_refused=ok|no-representative-happy-promote-path|verdict=FAIL
```

### 7. Outcome progress — PASS

Command는 위 suite 1회 실행이다. 다음 test가 모두 `ok`였다.

- `test_run_outcome.RunOutcomeTests.test_status_does_not_call_zero_delta_progress_and_advises_only_after_repeat`
- `test_run_observe.RunObserveTests.test_status_does_not_use_task_count_as_progress`
- `test_run_observe.RunObserveTests.test_status_renderer_names_objective_and_delta_before_audit`

두 개의 completed outcome pair를 통해 `no-objective-delta`는 progress가 아니며, task count 변경과
무관하게 current objective/stage/last positive delta를 투영하고 반복된 ledger evidence 뒤에만 advisory를
표시함을 판별한다.

Observation preimage:

```text
E2E-7|test_run_outcome.RunOutcomeTests.test_status_does_not_call_zero_delta_progress_and_advises_only_after_repeat=ok|test_run_observe.RunObserveTests.test_status_does_not_use_task_count_as_progress=ok|verdict=PASS
```

## 2. Touched kernel seam regression

전체 command는 1회만 실행했고 exit 0, `Ran 224 tests in 70.645s`, `OK`였다. 224라는 수는 결과의
설명일 뿐 exit 성공 조건으로 사용하지 않았다.

같은 실행에서 기존 fault fixture가 suite manifest에 살아 있고 실제로 `ok`임을 확인했다.

- Store: transactional bootstrap/WAL refusal, concurrent CAS loser zero-partial-write,
  append-only trigger, artifact symlink/no-follow, corruption/tamper fixtures
  (`test_run_store.RunStoreTests.*`, `test_run_store.ArtifactStoreTests.*`).
- Effects: all effect kinds crash-before/crash-after reconciliation,
  `test_fixture_3_exited_unreconciled_runner_completes_exactly_once`, marker identity/output proof,
  stale principal, Git/artifact conflict and CAS fixtures (`test_run_effects.RunEffectTests.*`).
- Verify: PC16 binary/tamper, PC20 actor/read-only verifier and terminal evidence, PC21 decision
  lineage, PC17/PC22 apply/CAS/tampered approval bundle fixtures (`test_run_verify.RunVerifyTests.*`).

재현 가능한 suite membership command:

```console
$ rg -n '^class |^    def test_' scripts/tests/test_run_store.py scripts/tests/test_run_effects.py scripts/tests/test_run_verify.py
# exit 0
```

## 3. Real backend smoke — FAIL

### 3.1 실행과 frozen inputs

실 repo가 아닌 다음 temp fixture에서 1회만 실행했다.

```text
/var/folders/zs/3v2hb5091zn7gtpdy5r1b17h0000gn/T/waystone-013-smoke-uapptx4j/repo
```

Probe command와 최종 exit:

```console
$ uv run python .gate013_smoke_probe.py
# exit 1
```

Probe는 일회성 검증 파일이어서 실행 뒤 worktree에서 삭제했다. 정확한 backend argv와 semantic prompt는
temp fixture의 immutable supervisor launch record에 남아 있다. 아래 command로 같은 argv를 재도출할
수 있다.

```console
$ jq -r '.argv | @sh' /var/folders/zs/3v2hb5091zn7gtpdy5r1b17h0000gn/T/waystone-013-smoke-uapptx4j/repo/.waystone/supervisors/400c9e18e5d879308eae4099d47dc3889801724da2644091aa20361f29395a41.launch.json
# exit 0
```

실제 child는 다음 backend 설정이었다.

```text
codex exec -m gpt-5.6-luna -c model_reasoning_effort=low --sandbox workspace-write --ephemeral --output-schema <temp-schema> -o <temp-repo>/WAYSTONE_RESULT.yaml <semantic-prompt>
```

- `run start`: exit 0
- run id: `019f87d8-48c9-7cbc-bc4a-2679c3373d16`
- backend session id: `019f87d8-4ba7-72c0-a369-bc4f9f2c3301`
- semantic prompt digest: `sha256:4caad9dedcef786f184f9d555cc3bccde4feb6e61d71141b0f42746d99424921`
- response schema digest: `sha256:d4f9074126e54e5ac09477556f0435ab3dfd0a76db47a2dec2980f5137024851`

### 3.2 요구된 세 digest

| Artifact | Digest |
|---|---|
| RunSpec | `sha256:33f23352b0d0806da8ab87142f11c3fd8eaffad96dc6248be9fff586c8cba658` |
| WorkBrief | `sha256:8b2fe276d034180de218e4ca6a209b27f8715c4154df32df8aac8347a0cfe1d8` |
| OutcomeDelta | **없음 — marker 미발행으로 candidate/evaluate/close 단계에 도달하지 못함** |

Artifact bytes 재검증 command:

```console
$ shasum -a 256 /var/folders/zs/3v2hb5091zn7gtpdy5r1b17h0000gn/T/waystone-013-smoke-uapptx4j/repo/.waystone/artifacts/sha256-33f23352b0d0806da8ab87142f11c3fd8eaffad96dc6248be9fff586c8cba658 /var/folders/zs/3v2hb5091zn7gtpdy5r1b17h0000gn/T/waystone-013-smoke-uapptx4j/repo/.waystone/artifacts/sha256-8b2fe276d034180de218e4ca6a209b27f8715c4154df32df8aac8347a0cfe1d8
# exit 0; filename과 동일한 두 digest
```

### 3.3 실패 관측

backend는 즉시 exit 1/HTTP 400을 반환했다.

```text
invalid_json_schema: In context=('properties', 'attempt_id'), schema must have a 'type' key.
```

이는 probe가 만든 response schema에서 `const`만 쓰고 `type`을 빠뜨린 검증 절차 오류다. product
success로 오인하지 않았고 §9.2 상한 때문에 schema를 고쳐 smoke를 재시도하지 않았다. child stdout과
stderr CAS digest는 각각 다음과 같다.

- stdout: `sha256:e8343981ba5b2ed972da246902de2896bd587a7d7767fb79013e086ccbcf22f5`
- stderr: `sha256:e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855`

Supervisor는 stdout/stderr를 CAS에 쓴 뒤 missing `WAYSTONE_RESULT.yaml`을 adapt하다 예외가 나서 marker
v2와 wait receipt를 발행하지 못했다. supervisor와 child PID는 모두 종료됐지만 DB는 다음과 같이 남았다.

```text
run=dispatch-ready(v2), job=running(v1), attempt=running(v1), action=effect(v3)
```

재현 가능한 readback:

```console
$ sqlite3 -header -column /var/folders/zs/3v2hb5091zn7gtpdy5r1b17h0000gn/T/waystone-013-smoke-uapptx4j/repo/.waystone/state.db 'select run_id,state,version from runs; select job_id,state,version from jobs; select attempt_id,state,version from attempts; select action_id,state,version from actions;'
# exit 0
$ test ! -e /var/folders/zs/3v2hb5091zn7gtpdy5r1b17h0000gn/T/waystone-013-smoke-uapptx4j/repo/WAYSTONE_RESULT.yaml && test ! -e /var/folders/zs/3v2hb5091zn7gtpdy5r1b17h0000gn/T/waystone-013-smoke-uapptx4j/repo/.waystone/runner-completions/400c9e18e5d879308eae4099d47dc3889801724da2644091aa20361f29395a41.json
# exit 0
```

따라서 marker v2 -> candidate publish -> `run close --outcome` -> ledger commit은 실행되지 않았다.
Read-only status 관측은 실행했으며 exit 0이었다.

```console
$ WAYSTONE_HOME=/var/folders/zs/3v2hb5091zn7gtpdy5r1b17h0000gn/T/waystone-013-smoke-uapptx4j/machine uv run python scripts/waystone.py status --project /var/folders/zs/3v2hb5091zn7gtpdy5r1b17h0000gn/T/waystone-013-smoke-uapptx4j/repo --json
# exit 0
```

투영은 active explore run `dispatch-ready`, current objective `hypothesis/solver`,
`outcome.last_delta=null`, `outcome.last_positive_delta=null`, advisory null이었다. smoke 자체는 요구된
완주를 하지 못했으므로 FAIL이다.

## 4. 독립 적대 리뷰

### 4.1 6축 공격 결과

| 축 | 공격 결과 |
|---|---|
| Project intent / authority | hypothesis/open question -> promotion criterion은 `StageModeRefusal`; coordinator summary는 prompt에서 “interpretation, not owner authority”로 렌더되고 owner-source source kind가 제한된다. 직접 우회 finding 없음. |
| Review / disposition | ingest는 immutable claim만 만들고, task materialization은 최신 validation에 결속된 명시적 `fix-now`/`fix-before-promotion` disposition에서만 가능하다. confirmed finding -> task 자동 전이 없음. |
| Test lifecycle / assurance | explore plan은 probe optional, permanent regression/ADR/verifier/macro review not-required다. probe -> permanent test 자동 전이 없음. |
| Execution / context | public stage executor 조립 부재, completed result lineage 누락, failed-child terminalization 부재: `G013-01`, `G013-02`, `G013-04`. |
| Stage / promotion lineage | pure chain validator는 reset을 거부하지만 production cycle head가 다음 run으로 운반되지 않는다: `G013-03`. evaluate mutation과 required promotion record 생략은 typed refusal한다. |
| Cutover / legacy / progress | objective-first status contract는 focused test로 성립한다. config normalizer에 legacy compatibility가 남았다: `G013-05`. |

금지된 자동 전이 4종의 결론:

1. hypothesis -> requirement: 막힘 (`completion.py:565-620`, focused refusal green).
2. confirmed finding -> task: 막힘 (`review_group.py:208-247`, 명시적 disposition/materialize만 허용).
3. probe -> permanent test: 막힘 (`assurance.py:323-350`, explore probe optional/regression not-required).
4. coordinator summary -> owner authority: 막힘 (`work_brief.py:313-348, 644-657`, source-kind 제한과 명시적 label).

아래 finding은 gate 문서 안의 **claim**일 뿐 task가 아니다. gate reviewer는 main의 disposition authority를
대신하지 않으므로 모든 latest disposition은 `없음(unadjudicated)`이다.

Claim code evidence의 공통 재현 command와 exit code:

```console
$ nl -ba waystone/cli/run_group.py | sed -n '315,365p;484,508p'; nl -ba waystone/runs/engine.py | sed -n '306,362p;460,580p;716,806p'; nl -ba waystone/runs/outcome.py | sed -n '571,599p'; nl -ba waystone/runs/supervisor.py | sed -n '1344,1403p'; nl -ba waystone/project/__init__.py | sed -n '490,541p'; nl -ba scripts/tests/test_run_outcome.py | sed -n '103,125p'
# exit 0
```

### G013-01 — public staged execution 조립이 없다

- Validity 주장: **confirmed**
- Claim: canonical `waystone run start/resume`와 `skills/run` 경로가 runner action 실행,
  WorkerResult 관측, frozen stage DAG 실행을 호출하지 않아 product run을 완료할 수 없다.
- Evidence: `run_group.py:484-508`은 start 뒤 반환하고 resume은 `StagedRunEngine.resume`만 호출한다.
  `engine.py:460-461`의 resume은 transport projection뿐이다. 실제 실행 함수
  `engine.py:467-580`은 caller-supplied handlers를 요구하지만 public CLI나 `skills/run/SKILL.md`에서
  호출되지 않는다. `rg -n 'execute_stage\(' waystone skills` 결과 production call site는 정의뿐이다.
- 제안 impact: **blocker** — e2e 5/6과 정상 real-backend completion의 public path가 없다.
- Latest disposition: **없음(unadjudicated)**

### G013-02 — completed WorkerResult가 outcome lineage에 등록되지 않는다

- Validity 주장: **confirmed**
- Claim: completed result를 관측해도 `worker-result:<final-attempt>` reference가 저장되지 않아
  `run close --outcome`이 항상 final result 부재로 거부된다.
- Evidence: `engine.py:306-362`의 `observe_worker_result`는
  `ContextRequestedWorkerResult`일 때만 worker-result/context-request references를 저장하고 completed
  branch는 그대로 반환한다. `engine.py:783-806`의 stage completion도 state transition만 기록한다.
  반면 `outcome.py:571-599`는 final attempt의 `worker-result:<attempt>`를 필수로 읽는다.
  `test_run_outcome.py:103-125` fixture는 이 reference를 수동 삽입하므로 production gap을 판별하지 않는다.
- 제안 impact: **blocker** — runner와 candidate가 성공해도 OutcomeDelta/ledger/status delta까지 닫히지 않는다.
- Latest disposition: **없음(unadjudicated)**

### G013-03 — review-cycle budget head가 production lineage에 영속되지 않는다

- Validity 주장: **confirmed**
- Claim: review cycle append 결과가 다음 run이 읽는 promotion lineage head에 연결되지 않아 새 promote
  run에서 consumed cycle count가 0으로 reset될 수 있다.
- Evidence: `engine.py:716-750`의 `append_review_cycle`은 cycle bytes를 CAS에 쓰고 반환할 뿐 store
  reference나 PromotionLineage revision을 기록하지 않는다. production call site도 없다.
  `run_group.py:332-360`은 새 promote run에서 evaluation producer의 기존
  `review_cycle_head_digest`만 상속하고 그 head에서 chain을 읽는다. evaluate 시작 시 head는 명시적으로
  null이다(`run_group.py:321-328`). pure compiler test가 공급받은 chain의 count reset을 거부하는 것과
  달리 공급할 durable head가 없다.
- 제안 impact: **major** — §9.1 promotion review budget을 새 run으로 우회할 수 있다.
- Latest disposition: **없음(unadjudicated)**

### G013-04 — failed child가 marker/state를 종결하지 않는다

- Validity 주장: **confirmed**
- Claim: runner가 result control file 없이 실패하면 supervisor가 marker/wait receipt를 쓰기 전에
  예외 종료하여 run/job/attempt/action이 nonterminal 상태로 남는다.
- Evidence: real smoke의 child exit 1과 stdout CAS는 관측됐지만 marker/result file은 없고 DB는
  `dispatch-ready/running/running/effect`다. `supervisor.py:1344-1366`은 stdout/stderr CAS 뒤 result를
  먼저 adapt하고, marker/write는 `supervisor.py:1367-1403`에 있어 missing result 예외가 terminal
  observation을 막는다.
- 제안 impact: **major** — backend/config failure가 영구 busy처럼 투영되고 정상 retry/close 근거가 없다.
- Latest disposition: **없음(unadjudicated)**

### G013-05 — legacy review/delegation config compatibility가 남아 있다

- Validity 주장: **confirmed**
- Claim: D15 single cutover와 §5.5 legacy config-test 삭제에도 config normalizer가 legacy
  review/delegation schema를 기본값으로 만들고 일부 legacy key를 무시 후 제거한다.
- Evidence: `project/__init__.py:490-520`은 `review.mode=packet`, legacy literal reviewers와 review
  fields를 계속 수용한다. `project/__init__.py:521-541`은 `delegation.enabled/env_prep`을 기본값으로
  만들고 `delegation.codex_runner_verified`를 error가 아니라 ignore/pop한다. §5.5는 이 compatibility
  test group 삭제를 명시하고 D15는 compatibility alias/silent fallback을 금지한다.
- 제안 impact: **minor** — canonical config projection에 제거된 surface가 남고 typo/legacy input이
  fail-loud하지 않는다.
- Latest disposition: **없음(unadjudicated)**

Finding count: **5**. 이 수 자체는 성공/실패 지표가 아니다.

## 5. 삭제 audit와 kernel diff

### 5.1 §5.5 명시 삭제

Command:

```sh
for path in \
  scripts/tests/test_delegate_cli.py \
  scripts/tests/test_delegate_core.py \
  scripts/tests/test_delegate_lifecycle.py \
  scripts/tests/test_delegate_verify.py \
  scripts/tests/test_migrations.py
do
  test ! -e "$path" || exit 1
done
```

Exit 0. 다섯 파일은 모두 absent다.

`scripts/tests/test_project.py`에는 `ProjectSurfaceTests`만 남아 있고
`DashboardLockingTests`, `LaneTests`, `RoundCloseTests`, legacy review/delegation compatibility,
SSOT/round/task-count resume projection group이 없다.

```console
$ rg -n 'class (DashboardLockingTests|LaneTests|RoundCloseTests)|review.mode|delegation|codex_runner_verified|ResumeStartHereTests|StoragePathTests' scripts/tests/test_project.py
# exit 1 (기대한 no-match)
```

§5.5 보존 kernel modules(`test_run_store`, `test_run_lease`, `test_run_effects`,
`test_run_supervisor`, `test_run_cancel`, `test_run_transport`, `test_run_verify`,
`test_review_runs_layout`)은 모두 존재하고 suite에 포함돼 있다.

### 5.2 보존 kernel unrelated rewrite audit

Commands:

```console
$ git diff --check cecb7b3..b0635bd
# exit 0; no output
$ git diff --numstat cecb7b3..b0635bd -- waystone/runs/store.py waystone/runs/effects.py waystone/runs/lease.py waystone/runs/supervisor.py waystone/runs/cancel.py waystone/runs/transport.py waystone/runs/verify.py scripts/tests/test_run_store.py scripts/tests/test_run_effects.py scripts/tests/test_run_lease.py scripts/tests/test_run_supervisor.py scripts/tests/test_run_cancel.py scripts/tests/test_run_transport.py scripts/tests/test_run_verify.py
# exit 0
$ git log --format='commit %h %s' --stat cecb7b3..b0635bd -- <위 kernel path들>
# exit 0
```

관측된 kernel numstat:

| Path | + | - | 계획 처분 범위 |
|---|---:|---:|---|
| `waystone/runs/store.py` | 253 | 31 | A2 RunSpec/store v2, context CAS |
| `waystone/runs/transport.py` | 5 | 0 | A2 context/action typed transport |
| `waystone/runs/supervisor.py` | 90 | 3 | B2 marker v2 production adapter |
| `waystone/runs/effects.py` | 33 | 7 | B2 marker/result observation + C1 outcome artifact refs |
| `waystone/runs/verify.py` | 9 | 1 | B2 stage/result/evaluation binding |
| `scripts/tests/test_run_store.py` | 11 | 9 | store v2 focused seam update |
| `scripts/tests/test_run_verify.py` | 30 | 5 | B2 touched verify focused cases |
| lease/cancel 및 그 focused test | 0 | 0 | 보존, 미수정 |

Kernel 변경 commit은 `e50f282`(A2 store/context), `828bc54`(B2 assurance/marker),
`27e619f`(C1 outcome effect reference) 세 개뿐이다. 각 stat과 실제 변경 구획은 §5.1 kernel
처분표 및 A2/B2/C1 scope에 대응한다. 보존 kernel에서 unrelated rewrite는 관측되지 않았다.

삭제 audit 판정: **PASS**.

## 6. Gate 결론

- E2E: **4 PASS / 3 FAIL**; count가 아니라 4번의 budget-reset 경로와 5·6번의 public happy path
  부재가 exit 실패 근거다.
- Touched seam suite: **PASS**, 1회 실행, store/effects/verify fault fixtures green.
- Real backend smoke: **FAIL**, run id
  `019f87d8-48c9-7cbc-bc4a-2679c3373d16`; RunSpec/WorkBrief까지만 발행되고 OutcomeDelta 없음.
- Independent adversarial review: claim 5건, 모두 disposition 없음; task로 자동 전환하지 않음.
- 삭제/kernel audit: **PASS**.

따라서 `b0635bd`는 이 evidence 기준으로 0.13 exit gate를 통과하지 않는다.
